path: /Shared/swamp-dlt-smoke
language: PYTHON
overwrite: true
content: |
  # Databricks notebook source
  import dlt
  from pyspark.sql import functions as F

  @dlt.table(comment="swamp dlt smoke test, produces one row")
  def hello_swamp():
      return spark.range(1).select(F.lit("hello from swamp dlt").alias("msg"))
